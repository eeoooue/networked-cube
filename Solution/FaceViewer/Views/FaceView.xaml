<UserControl x:Class="FaceViewer.Views.FaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels="clr-namespace:FaceViewer.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance viewModels:FaceViewModel}">

    <Grid>

        <StackPanel Orientation="Vertical" VerticalAlignment="Center">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" >
                <Button Height="40" Width="40" Margin="5" Background="{Binding ColourR0C0}"></Button>
                <Button Height="40" Width="40" Margin="5" Background="{Binding ColourR0C1}"></Button>
                <Button Height="40" Width="40" Margin="5" Background="{Binding ColourR0C2}"></Button>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" >
                <Button Height="40" Width="40" Margin="5" Background="{Binding ColourR1C0}"></Button>
                <Button Height="40" Width="40" Margin="5" Background="{Binding ColourR1C1}"></Button>
                <Button Height="40" Width="40" Margin="5" Background="{Binding ColourR1C2}"></Button>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" >
                <Button Height="40" Width="40" Margin="5" Background="{Binding ColourR2C0}"></Button>
                <Button Height="40" Width="40" Margin="5" Background="{Binding ColourR2C1}"></Button>
                <Button Height="40" Width="40" Margin="5" Background="{Binding ColourR2C2}"></Button>
            </StackPanel>
        </StackPanel>

    </Grid>
</UserControl>
